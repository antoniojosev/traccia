ADMIN_TOKEN=change-me-to-a-long-random-string

# Required by docker-compose.prod.yml (optional in local dev — see README).
# SESSION_SECRET=change-me-to-a-different-long-random-string
# POSTGRES_PASSWORD=change-me-prod-database-password

# Optional — see README for defaults/fallbacks if left unset.
# RATE_LIMIT_PER_MINUTE=120
# LOGIN_RATE_LIMIT_PER_MINUTE=10
# PLUGINS_DIR=./plugins
# GEOIP_DB_PATH=./GeoLite2-City.mmdb
